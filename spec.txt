I'm calling an API, and using a Python script to loop through the response dictionaries, assigning them to their own objects, sending them as created schema/tables in DuckDB, then calling a dbtRunner to perform transformations on the DuckDB data. Then finally sending the transformed data to a BI tool. The only thing I really need to understand here is if Airflow is better capable for a task that can help to cut redundancy, and which BI tool or app is best for a front-facing analytics engineering portfolio that I'm showing to hiring managers. I'm thinking of going for Python Dash, but if I can use PowerBI as a free tool in a similar vein to Dash, or Streamlit then I would prefer either that or Tableau.  